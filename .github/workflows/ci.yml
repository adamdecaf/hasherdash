name: CI

on:
  push:
    branches: [main, master]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  DOCKER_IMAGE: adamdecaf/hasherdash
  ASIC_RS_REPO: https://github.com/adamdecaf/asic-rs.git
  ASIC_RS_REF: feat/go-bindings

jobs:
  build:
    name: Build and test
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout hasherdash
        uses: actions/checkout@v7

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            cmake \
            pkg-config

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          check-latest: true
          cache-dependency-path: go.sum

      - name: Checkout asic-rs (Go bindings + FFI)
        uses: actions/checkout@v7
        with:
          repository: adamdecaf/asic-rs
          ref: feat/go-bindings
          path: .deps/asic-rs

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: .deps/asic-rs -> target

      - name: Build asic-rs FFI
        run: |
          make -C .deps/asic-rs/go ffi
          go mod edit -replace=github.com/256foundation/asic-rs/go="${GITHUB_WORKSPACE}/.deps/asic-rs/go"
          echo "CGO_CFLAGS=-I${GITHUB_WORKSPACE}/.deps/asic-rs/go/asicrs/include" >> "$GITHUB_ENV"
          echo "CGO_LDFLAGS=-L${GITHUB_WORKSPACE}/.deps/asic-rs/go/asicrs/lib -lasic_rs_ffi -lm -ldl -lpthread" >> "$GITHUB_ENV"
          echo "LD_LIBRARY_PATH=${GITHUB_WORKSPACE}/.deps/asic-rs/go/asicrs/lib" >> "$GITHUB_ENV"

      - name: Unit tests (no cgo)
        env:
          CGO_ENABLED: "0"
        run: go test ./internal/config ./internal/store ./internal/axetemp -count=1

      - name: Build hasherdash
        env:
          CGO_ENABLED: "1"
        run: go build -trimpath -o bin/hasherdash ./cmd/hasherdash

      - name: Verify binary links
        run: |
          test -x bin/hasherdash
          file bin/hasherdash
          ldd bin/hasherdash | tee /tmp/ldd.txt
          if grep -q asic_rs_ffi /tmp/ldd.txt; then
            echo "linked against libasic_rs_ffi (shared)"
          else
            echo "no shared asic_rs_ffi in ldd (likely static .a) — ok if binary built"
          fi

  docker:
    name: Docker image
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout hasherdash
        uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      # On release tags (v*), publish both:
      #   adamdecaf/hasherdash:<version>  (e.g. 1.0.0 from tag v1.0.0)
      #   adamdecaf/hasherdash:latest
      # On branch/PR: build only as :ci for smoke test (no Hub push).
      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.DOCKER_IMAGE }}
          tags: |
            type=semver,pattern={{version}}
            type=raw,value=latest,enable=${{ github.ref_type == 'tag' }}
            type=raw,value=ci,enable=${{ github.ref_type != 'tag' }}

      - name: Login to Docker Hub
        if: github.ref_type == 'tag'
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push release image
        if: github.ref_type == 'tag'
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            ASIC_RS_REPO=${{ env.ASIC_RS_REPO }}
            ASIC_RS_REF=${{ env.ASIC_RS_REF }}
          cache-from: type=gha,scope=hasherdash-docker
          cache-to: type=gha,mode=max,scope=hasherdash-docker

      - name: Build image (CI smoke)
        if: github.ref_type != 'tag'
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile
          push: false
          load: true
          tags: ${{ env.DOCKER_IMAGE }}:ci
          build-args: |
            ASIC_RS_REPO=${{ env.ASIC_RS_REPO }}
            ASIC_RS_REF=${{ env.ASIC_RS_REF }}
          cache-from: type=gha,scope=hasherdash-docker
          cache-to: type=gha,mode=max,scope=hasherdash-docker

      - name: Smoke-test image
        if: github.ref_type != 'tag'
        run: |
          docker run --rm --entrypoint /usr/local/bin/hasherdash ${{ env.DOCKER_IMAGE }}:ci -h 2>&1 | head -20 || true
          docker run --rm --entrypoint /bin/sh ${{ env.DOCKER_IMAGE }}:ci -c '
            set -e
            test -x /usr/local/bin/hasherdash
            test -f /usr/local/lib/libasic_rs_ffi.so
            ldd /usr/local/bin/hasherdash | grep -q asic_rs_ffi
            echo "docker image ok"
          '
